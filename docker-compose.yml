version: "3.9"

services:
  exec-lane:
    build:
      context: .
      dockerfile: exec-lane/Dockerfile
    container_name: tradingview-gmocoin-bot
    env_file:
      - ./config/.env
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
