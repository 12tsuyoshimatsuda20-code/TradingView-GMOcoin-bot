version: '3.9'

services:
  bot:
    build: .
    container_name: tradingview-gmocoin-bot
    restart: unless-stopped
    env_file:
      - ./config/.env
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
    user: "1000:1000"
